{
  "name": "screeps-ai-typescript-starter",
  "version": "1.0.0",
  "description": "A starter kit for developing a Screeps AI using TypeScript.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "mocha -r ts-node/register test/**/*.test.ts"
  },
  "dependencies": {
    "typescript": "^4.0.0"
  },
  "devDependencies": {
    "ts-node": "^9.0.0",
    "mocha": "^8.0.0"
  }
}
